{
  "name": "cypress-treino",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "type": "commonjs",
  "devDependencies": {
    "@faker-js/faker": "^10.3.0",
    "cypress": "^15.13.0",
    "cypress-if": "^1.13.2",
    "cypress-xpath": "^2.0.1",
    "mochawesome": "^7.1.4",
    "mochawesome-merge": "^5.1.1",
    "mochawesome-report-generator": "^6.3.2"
  },
  "dependencies": {
    "dotenv": "^17.4.2",
    "faker": "^5.5.3"
  },
  "scripts": {
  "delete:reports": "rm -rf cypress/reports/*",
  "mochawesome:merge": "npx mochawesome-merge cypress/reports/*.json > cypress/reports/report.json",
  "mochawesome:generate": "npx marg cypress/reports/report.json --reportDir cypress/reports --reportFilename index.html",
  "test:regression": "npm run delete:reports && npx cypress run && npm run mochawesome:merge && npm run mochawesome:generate"
}
}
